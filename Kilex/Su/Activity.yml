info:
  name: Activity
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}/v1/su/activity"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
