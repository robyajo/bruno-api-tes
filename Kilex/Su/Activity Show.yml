info:
  name: Activity Show
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain}}/v1/su/activity/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
