info:
  name: Errors
  type: http
  seq: 3

http:
  method: GET
  url: "{{domain}}/v1/su/monitor/errors"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
