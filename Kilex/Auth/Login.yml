info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{domain}}/v1/login"
  body:
    type: json
    data: |-
      // Su
      {
        "username":"robyfull.dev",
        "password":"Password123"
      }
      // Admin

      // {
      //   "email":"roby@mituni.id",
      //   "password":"Password123"
      // }

      // User

      // {
      //   "email":"user@mituni.id",
      //   "password":"Password123"
      // }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();

        if (data.success && data.data.tokens) {

            bru.setEnvVar("tokens", data.data.tokens.accessToken);

            console.log("Token saved:", data.data.tokens.accessToken);

        }
        if (data.success && data.tokens) {

            bru.setEnvVar("refresh-token", data.data.tokens.refreshToken);

            console.log("Token saved:", data.data.tokens.refreshToken);

        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
