info:
  name: Me
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain}}/v1/me"
  auth:
    type: bearer
    token: "{{tokens}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
