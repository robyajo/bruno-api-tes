info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{domain}}/v1/logout"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
