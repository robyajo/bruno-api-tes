info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: https://dummy-api.lapeh.web.id/api/auth/login
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
