info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: https://dummy-api.lapeh.web.id/api/auth/register
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
