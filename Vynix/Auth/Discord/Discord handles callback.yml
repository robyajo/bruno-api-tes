info:
  name: Discord handles callback
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain}}/v1/auth/discord/callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
