info:
  name: REGISTER
  type: http
  seq: 1

http:
  method: POST
  url: "{{domain}}/v1/auth/register"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "email":"rosdbsyfusll.dev@gmail.com",
        "name":"rosdys",
        "username":"rbsyds",
        "password":"password123",
        "password_confirmation":"password123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();

        if (data.success && data.data.accessToken) {

            bru.setEnvVar("TOKEN", data.data.accessToken);

            console.log("Token saved:", data.data.accessToken);

        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
