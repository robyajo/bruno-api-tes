info:
  name: Google consent screen
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain}}/v1/auth/google"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
