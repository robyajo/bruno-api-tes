info:
  name: Google handles callback
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}/v1/auth/google/callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
