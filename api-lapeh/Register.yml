info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_prod}}/api/auth/register"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
